use vemodel::AccountId;

pub(crate) fn transfer(recipient: AccountId, amount: u64) -> Result<(), String> {
    Ok(())
}

pub(crate) fn agent_balance() -> u64 {
    0
}

pub(crate) fn balance_of(account: AccountId) -> u64 {
    0
}
